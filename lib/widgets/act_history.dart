import 'package:flutter/material.dart';

class ActHistory extends StatelessWidget{
  const ActHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      // children: const [
      //   Text('知名作品', style: TextStyle(fontSize: 20)),
      //   SizedBox(height: 8),
      //   Text('想見你'),
      // ],
    );
  }
}